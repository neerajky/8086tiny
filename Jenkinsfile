node {
   stage('Preparation') {
     echo 'Preparation'
   }
   stage('Build') {
      if (isUnix()) {
         echo "Is Unix"
      } else {
         echo "Something Else"
      }
   }
   stage('Results') {
     echo "this is result"
   }
}
